# 更新日志

本文件记录**官方发行版**的变化。版本号 / 预发布标记 / 构建次数由 `toolkit_base.py` 的四行统一管理，
用法见 README「构建发行版」一节。

---

## 1.1.0 · 2026-09-17（build_58）

### 新增

- **SquashFS 尺寸读取改为纯 Python 解析元数据**：不再调用外部 `sqfs2tar` 把整个镜像流一遍
  （2.3 GB 镜像原先要约 99.6 s，现在 0.01~0.24 s）。支持 lz4 / gzip / xz / zstd。
- **SquashFS 提取改为逐个 `rdsquashfs --cat`**（0.032 s/个）：不再把整包 tar 收进内存
  （8 GB 级镜像会直接 MemoryError），并修掉「空文件被算成失败」。
  → 两条合起来：**`sqfs2tar.exe` 已从依赖与仓库中移除**，不会再有安全软件按文件拦截它。
- **插件 API 扩展**：`api.ui` 新增 `drop_box`（只读拖入框，Tk 用 tkinterdnd2、Qt 用只读
  QLineEdit 真接 DnD）与 `set_state`（置灰）—— 插件做拖拽 / 禁用不必再自己 import tkinter。
- **视频转换**：ffmpeg 只在「程序目录」与「`user.ltx` 手动指定」两处查找，不再扫 PATH 与常见目录；
  找不到时弹出带按钮的窗口 —— **自动安装 / 手动选择 / 稍后**，只有点「自动安装」才联网下载
  （优先用项目自带的 `download_ffmpeg.py`，ffmpeg + ffprobe 一起装；退回 imageio-ffmpeg 时缺
  ffprobe 会如实报出，不假装成功）。
- **文本提取**：输入框改为「选择待处理的 `scripts/` 或 `gameplay/` 文件夹」，并且**只认这个文件夹本身**
  （不再向上/向下猜测模组结构；选错会明确提示）。
- **汉化包生成**：渲染字号按**墨迹**定标放大到贴满格子（格子表/行高/字宽表一个字不动）；
  简体缺字形时用**繁体字形**补（OpenCC ST 表派生 2640 条，码位不变）；字体选择器新增
  「日语字体」第四态，列表与预览都会标出。
- **构建器**：版本号 / 预发布标记 / 两个构建计数写回；操作栏固定在窗口底部。
- 编码转换的任务壳统一到共享 `TaskRunner`（忙碌态、失败路径、进度起停只有一处实现）。

### 修复

- **滚动条一族（本轮主要工作）**
  - 「该出现时不出现」—— 滚动条被隐藏后放回时只拿到 1×1（存在但看不见，pack 顺序 + 吞异常所致）；
  - 「不该滚的时候也能滚」—— 面板 `fit` 用错，内容自然高被树的最小请求高顶起来（矮窗口下
    明明没内容也有隐藏滚动范围）；
  - 「两个滚动条同时动」—— 同一次滚轮事件只允许一个控件滚动，且**隐藏的滚动条不参与**；
  - 页面滚动把顶部**标签条一起滚走**；
  - 面板右侧边框消失、底部横向滚动条常驻；
  - 文件系统页两棵树补上**横向滚动条**（树是路径列表，横向被裁就是丢信息）；
  - 树行对齐改为以勾选框为准。
  - 以上规则已做成**结构强制**（`bar_shown` 四看判定 + `guard_wheel` 统一入口 + 三类锁）。
- **任务结束后状态栏停在「加载中…」**：`TaskRunner` 现在恢复任务开始前的状态
  （App 自报「完成 · N 条」与失败提示优先，不被覆盖）。
- **构建器「开始构建…」操作栏被窗口切掉**（矮窗口下整条不可见）。
- 视频页构造期探测 ffmpeg 拖慢启动（构造耗时 23~39 ms → 7.5~13.6 ms，改到动作入口惰性探测）。
- 外部工具输出按 UTF-8 解码（此前中文日志乱码）。

### 优化

- 启动观感：标签条一次建齐、启动期隐藏窗口装配 —— 不再「顶上的栏一个一个弹出来」。
- 面板高度随内容（空面板不再占一大块）。
- 分隔条拖动改为**填色带预览 + 松手一次重排**（拖动期间不重排几何，避免卡顿）。
- 日志栏可**完全收起**，且改变窗口尺寸不会再弹回来。
- 清理死代码（不可达的冻结档、旧滚轮方法）。

### 工程与测试

- 新增 `run_scroll_audit.py`：真页面滚动条几何审计（六个栏目 × 多档窗口尺寸 × 宽窄往返）。
- 新增 `run_inject_layout_locks.py`：**44 条注入验证**（先证明锁会红，再证明修好即绿；带启动自检）。
- 新增 Qt 试点探针（`run_qt_pilot_probe.py` / `run_qt_build_probe.py`）与 PySide6 试点外壳。
- 静态守卫（挂载点名单从常量表派生，不手抄）+ 自测；行尾声明（`.editorconfig` / `.gitattributes`）。
- `run_ci.py` 三档（默认 / `--fast` / `--release`），SKIP 与 PASS 分开统计并列出「本次未覆盖」。

---

## 1.0.0 · 2026-08-16

首个正式版：六个工具统一 Hub（文件系统 / 编码转换 / 文本提取 / XML 校对 / OGM 视频转换 /
汉化包生成）、X-Ray DB 六格式解包与打包、SquashFS 支持、插件系统（栏目 / 解密 / 新格式 /
右键菜单 / 下拉项）、亮暗主题与 `user.ltx` 持久化、Win11 标题栏跟随色板。
详见 GitHub Release `v1.0.0`。
